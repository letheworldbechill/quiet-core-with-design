/**
 * Publish screen.
 * Final step with share placeholders.
 */

import { MediaPlaceholder, SharePlaceholder } from "../components";

export function Publish() {
  return (
    <main>
      <h2>Veröffentlichen</h2>

      <MediaPlaceholder />
      <SharePlaceholder />
    </main>
  );
}
